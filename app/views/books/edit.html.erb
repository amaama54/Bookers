<div class="body">
  <h1>Editing Book</h1>
    <% if @book.errors.any? %>
        <%= @book.errors.count %>errors prohibited this book from being saved:
        <% @book.errors.full_messages.each do |message| %>
        <ul>
          <li><%= message %></li>
        </ul>
              <% end %>
    <% end %>
  <%= form_with model:@book, local: true do |f| %>
    <div class="input">
      <p>Title</p>
      <%= f.text_field :title %>
    </div>
    <div class="input">
      <p>Body</p>
      <%= f.text_area :body %>
    </div>
    <div class="btn">
      <%= f.submit "Update Book" %>
    </div>
    <div class="show-link">
      <p></p><%= link_to "Show", "/books/#{@book.id}" %></p>
      <p>|</p>
      <p></p><%= link_to "Back", "/books" %></p>
    </div>
  <% end %>
</div>